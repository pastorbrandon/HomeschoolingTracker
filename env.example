# HomeSchool Tracker Environment Variables
# This app doesn't require any environment variables as it runs fully offline
# with local IndexedDB storage.

# Optional: Analytics (if you want to add Google Analytics later)
# NEXT_PUBLIC_GA_ID=your-google-analytics-id

# Optional: Sentry (if you want to add error tracking later)
# NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn

# Optional: Custom domain (for deployment)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
